# pharmacy-dbms-project
Online Pharmacy Management System using DBMS. This project includes database design, SQL queries (SELECT, JOIN, Aggregate, Subquery, INSERT, UPDATE, DELETE), normalization up to 3NF, and screenshots of outputs.
